{
    "configurations": [
        {
            "type": "hl",
            "request": "launch",
            "name": "Launch HL",
            "cwd": "${workspaceFolder}",
            "program": "out/out.hl"
        }
    ]
}