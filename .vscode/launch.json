{
    "configurations": [
        {
            "type": "hl",
            "request": "attach",
            "name": "Launch HL",
            "cwd": "${workspaceFolder}",
            "port": 6112
        }
    ]
}